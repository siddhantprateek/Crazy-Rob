#include <bits/stdc++.h>

using namespace std;
#define ar array
#define ll long long

int main()
{
    int t, n, m, r[], b[];
    cin >> t;

    while (t--)
    {
        cin >> n;
        for (int j = 0; j < n; j++)
            cin >> r[j];
        cin >> m;
        for (int j = 0; j < m; j++)
            cin >> b[j];
    };
    return 0;
}