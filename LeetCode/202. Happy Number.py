class Solution:
    def isHappy(self, n: int) -> bool:


        # power_add = 0
        # if n == 1:
        #     return True
        # if n != 1:
        #     for item in str(n):
#                 power_add = power_add + int(item)**2
#             print(power_add)
#             return self.isHappy(power_add)


# use linked list check whether cycle is present if take input as 12 if the cycle repeats the
# slow and fast  if we get the number check whether it is happy number or not.

