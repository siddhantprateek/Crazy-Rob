#include <bits/stdc++.h>
#include <vector>

using namespace std;

void permute(vector<int> unprocessed, vector<int> processed)
{
    if(unprocessed == None)
    {
        for(int i = 0; i < sizeof(processed)/sizeof(processed[0]); i++)
        {
            cout << processed[i];
        }
    }
    for(int i = 0 ; )

}

int main()
{

    return 0;
}